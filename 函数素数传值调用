#define _CRT_SECURE_NO_WARNINGS 1
#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
#include <windows.h>
#include<string.h>
#include <math.h>
#include<windows.h>
int is_prime(int n)
{
	int a = 0;
	for (a = 2; a <=n; a++)
	{
		if (n%a == 0)
			return 0;
		else
			return 1;
	}
}
int main()
{
	int i = 0;
	for (i = 100; i <= 200; i++)
	{
		if (is_prime(i) == 1)
			printf("%d ", i);
	}
	return 0;
}
